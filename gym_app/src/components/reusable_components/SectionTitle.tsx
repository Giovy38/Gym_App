

export default function SectionTitle({ title }: { title: string }) {
    return (
        <div>
            <h2 className="font-logo-font text-center text-4xl text-[#f8bf58]">{title}</h2>
        </div>
    )
}